{
  "endpoint": {
    "method": "GET",
    "path": "/api/v1/base-data-hotel/locations/regions/{regionCode}"
  },
  "cases": [
    {
      "name": "an existing record",
      "category": "happy_path",
      "path_params": {
        "regionCode": "{{seed}}"
      },
      "expected_status": 200,
      "reason": "Fetching a record that exists should return it."
    }
  ]
}
